-- migration_v5.sql (DEPRECADO / NÃO USAR)
--
-- Esta migração foi criada para reduzir os status de 6 para 3, mas a decisão
-- foi revertida — o projeto segue com os 6 status originais. Não rode este
-- arquivo. O schema.sql (v4) já tem tudo que precisa.
--
-- Mantido só como histórico, vazio de propósito.
select 1;
